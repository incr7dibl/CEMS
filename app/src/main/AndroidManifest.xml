<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.cems">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".HomePage" />
        <activity android:name=".KshitijActivity" />
        <activity android:name=".VirtuosicActivity" />
        <activity android:name=".CsaActivity" />
        <activity android:name=".MesaActivity" />
        <activity android:name=".EsaActivity" />
        <activity android:name=".OtherActivity" />

        <activity android:label="@string/app_name" android:name=".EventMain"></activity>
    </application>

</manifest>